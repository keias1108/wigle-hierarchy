Original prompt: ui가 안 깔끔한 부분들이 있어. 설명도 굳이 넣을 필요 없는 것도 많고, 캔버스도 차트에 가려져. Playwright MCP를 사용하여 내 웹사이트를 테스트하세요. 웹사이트를 열고 메인 페이지를 클릭해보고 잘못된 부분이 없는지 확인하세요.

- 2026-03-07: Playwright CLI on WSL could not use Windows Chrome. Installed temporary Playwright package under `/tmp/pwtest` to test the local site from WSL without modifying the project dependencies.
- 2026-03-07: Cleaned the `wigle-u-2d` UI around hierarchy-first use. Removed the broken logo dependency and extra explainer card, added an inline SVG favicon, reduced sidebar noise, made the chart toggle opt-in, and compacted the recording controls so they no longer overlap the canvas.
- 2026-03-07: Verified with Playwright in WSL using the cached Chromium binary. No console errors or 404s remained after the cleanup. Main page interactions tested: chart toggle, mode button click, canvas click. Final artifact screenshots: `output/playwright/ui-main.png`, `output/playwright/ui-interacted.png`.
- 2026-03-07: Reworked hierarchy dynamics toward recursive promotion + re-differentiation. In `src/utils/shaderLoader.js`, promoted echo now suppresses fresh binding at the center and boosts new binding at echo edges via `edgeDifferentiation` instead of directly amplifying the same center. Added a small noise term at echo edges to help spontaneous sibling emergence.
- 2026-03-07: Retuned defaults/presets for multi-center behavior: stricter `hierarchyAlignment`, lower `unitDecay`, higher `promotionThreshold`, lower `coarseFeedback`, stronger `outerStrength`, slightly lower `diffusionRate`.
- 2026-03-07: Browser smoke test passed after the hierarchy rewrite. Updated params were present in `window.energyLifeSim.params`, no console errors were emitted, and a post-change artifact was saved to `output/playwright/hierarchy-recursive.png`. Headless capture still renders the simulation area as black, so field behavior itself still needs human validation in a real browser.
- 2026-03-10: Added a flip-aware hierarchy pass. The alpha channel now stores the previous frame's energy, hierarchy dynamics use a `flipSignal` and `phaseDifferentiation`, and the renderer exposes a new `Phase Flip` layer for inspecting alternating structures directly.
- 2026-03-10: Verified the new layer in-browser. `#layerView=phase` selects `viewMode=5`, the page loads without console errors, and artifacts were saved to `output/playwright/phase-flip-mode.png` and `output/playwright/phase-flip-mode.json`.
- 2026-03-10: Added a second slow `trait` compute field to move from pair-only hierarchy toward phylogenetic differentiation. `field` now depends on `trait`, and `trait` depends on both `field` and itself. The lineage RGB channels encode separator/laminar/branching tendencies and A stores niche memory.
- 2026-03-10: Added `lineagePressure`, `traitMutation`, and `nicheMemory` controls plus `Lineage` and `Niche` view layers. Composite view is also lightly tinted by lineage traits so divergent morphotypes can become visible without switching layers.
- 2026-03-10: Browser verification passed for the new phylogeny views. `#layerView=lineage` maps to `viewMode=6`, `#layerView=niche` maps to `viewMode=7`, and no console errors were emitted. Artifacts: `output/playwright/lineage-mode.png`, `output/playwright/niche-mode.png`, `output/playwright/phylogeny-modes.json`.
- 2026-03-10: Added the three substrate changes discussed after the banding/duality diagnosis. `field` now supports semi-asynchronous updates (`asyncMix` + per-cell phase gating), rotated neighborhood sampling (`rotatedKernelMix`), and a separate continuous flow field (`flow`) that advects the sampling kernel and is visible through a new `Flow` layer. `trait` also reads that flow so lineage drift can respond to local transport rather than only the square grid.
- 2026-03-10: Wired the new substrate parameters through UI, defaults, presets, and runtime uniform updates. Also fixed a control bug where slider changes were not propagating into the new `flow` compute variable.
- 2026-03-10: Retuned the initial substrate defaults to be more conservative after the first pass was too aggressive: weaker async gating, weaker rotated mixing, smaller flow seeding, and lower flow coupling/response defaults.
- 2026-03-10: Playwright verification after the substrate pass succeeded for UI wiring: `#layerView=lineage/niche/flow` map to `viewMode=6/7/8`, canvas click still works, and no console or page errors were emitted. Artifact set: `output/playwright/substrate-modes.png`, `output/playwright/substrate-modes.json`. Headless Chromium still shows the hierarchy composite near-black with `avgEnergy` rounded to `0.000`, so the new substrate defaults still need real-browser visual validation and likely additional tuning against actual user-observed behavior.
- 2026-03-10: Verified the user’s ignition workflow (`growthWidth` raised to `0.145`, then slowly lowered) in headless Chromium. The substrate changes do not currently create a stronger visible morphology and, when enabled by default, they reduce survival around the `growthWidth=0.05` region compared with a substrate-off control.
- 2026-03-10: Based on that comparison, left the three substrate features implemented but changed their defaults/presets back to `0`. This preserves the original hierarchy behavior as the baseline while keeping `Async Mix`, `Rotated Kernel`, and `Flow` available as explicit experimental controls instead of silently degrading the core behavior.
- 2026-03-10: Shifted focus from substrate tweaks to local rule divergence. `trait` now modulates actual local kernel/growth/promotion parameters in the hierarchy shader, and `niche` accumulation was tightened to be more pocket-like instead of diffusing into broad bands.
- 2026-03-10: Best headless checkpoint so far is the `lineage-pocket-check-2` capture set: `output/playwright/lineage-composite-2.png`, `output/playwright/lineage-view-2.png`, `output/playwright/niche-view-2.png`. It shows multiple surviving promoted structures and clearer lineage-localized islands than earlier attempts, though it is still not at the user’s target “phylogenetic divergence” feel.
- 2026-03-10: Tried an additional founder-drift promotion bias after that, but it degraded survival too much. Reverted that specific change and kept the prior local-rule-divergence state.
